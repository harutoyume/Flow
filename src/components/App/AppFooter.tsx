import React from "react";
import Link from "next/link";

const AppFooter = () => {
  return (
    <footer className="absolute bottom-0 w-full p-4 text-sm text-muted-foreground"></footer>
  );
};

export default AppFooter;
